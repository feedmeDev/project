<%= form_for(@allergen_component) do |f| %>
  <% if @allergen_component.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@allergen_component.errors.count, "error") %> prohibited this allergen_component from being saved:</h2>

      <ul>
      <% @allergen_component.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :allergen_warning %><br>
    <%= f.text_field :allergen_warning %>
  </div>
  <div class="field">
    <%= f.label :component %><br>
    <%= f.text_field :component %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
